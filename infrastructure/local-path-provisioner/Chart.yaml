apiVersion: v2
name: local-path-provisioner-wrapper
description: Wrapper chart for local-path-provisioner in the homelab cluster
type: application
version: 0.1.0

dependencies:
  - name: local-path-provisioner
    version: 0.0.32
    repository: https://charts.containeroo.ch
