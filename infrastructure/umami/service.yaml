apiVersion: v1
kind: Service
metadata:
  name: umami
  namespace: analytics
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app.kubernetes.io/name: umami
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: umami
  ports:
    - name: http
      port: 80
      targetPort: http
