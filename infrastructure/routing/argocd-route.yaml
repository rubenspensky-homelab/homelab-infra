apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: routing
  hostnames:
    - argocd.home.lab
  rules:
    - backendRefs:
        - name: argocd-server
          port: 443
